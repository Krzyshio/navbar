import React from "react";

function NavBarHeader() {
    return <div className="NavBarHeader" style={{ backgroundImage:  `url(${process.env.PUBLIC_URL + '/header-profile.png'})`}}>

    </div>
}

export default NavBarHeader